import React from 'react';
import './ReferenceSIF.scss';

const ReferenceSIF = ({ props, }) => {

    return (
                <>
                {/* <InputWrap labelText='Field Name'>
                    <InputText value= {formData.field_name || ''} handleChange= {(e) => {handleStringInputChange('field_name', e)}} required/>
                </InputWrap>
                <InputWrap labelText='Help Text'>
                    <InputText value= {formData.help_text || ''} handleChange= {(e) => {handleStringInputChange('help_text', e)}}/>
                </InputWrap> */}
                ReferenceSIF
                </>
    );
};

export default ReferenceSIF;